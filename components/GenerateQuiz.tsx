import React from "react";

const GenerateQuiz = () => {
  return (
    <div className="generate-container">
      <button className="generate-btn" type="submit">Generate quiz!</button>
    </div>
  );
};

export default GenerateQuiz;
